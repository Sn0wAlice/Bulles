<%- include('partials/head') %>
<%- include('partials/nav') %>

<main class="mx-auto px-6 lg:px-12 py-8 flex-1">
  <div class="flex items-center justify-between mb-8">
    <h1 class="text-2xl lg:text-3xl font-bold text-zinc-100"><%= title %></h1>
    <span class="text-base text-zinc-500"><%= bds.length %> tome<%= bds.length !== 1 ? 's' : '' %></span>
  </div>

  <!-- Filters -->
  <div class="flex flex-col sm:flex-row flex-wrap gap-4 mb-8">
    <input type="text" id="search-input" placeholder="Rechercher titre, série ou tag..."
           class="bg-zinc-900 border border-zinc-700 rounded-lg px-4 py-3 text-base text-zinc-200 placeholder-zinc-500 focus:outline-none focus:border-amber-600 w-full sm:w-80">
    <div class="flex gap-4 flex-wrap">
      <select id="filter-serie" class="bg-zinc-900 border border-zinc-700 rounded-lg px-4 py-3 text-base text-zinc-200 focus:outline-none focus:border-amber-600 flex-1 sm:flex-none">
        <option value="">Toutes les séries</option>
        <% for (const s of series) { %>
          <option value="<%= s %>"><%= s %></option>
        <% } %>
      </select>
      <select id="filter-status" class="bg-zinc-900 border border-zinc-700 rounded-lg px-4 py-3 text-base text-zinc-200 focus:outline-none focus:border-amber-600 flex-1 sm:flex-none">
        <option value="">Tous les statuts</option>
        <% for (const s of statuses) { %>
          <option value="<%= s.value %>"><%= s.label %></option>
        <% } %>
      </select>
      <% if (tags.length > 0) { %>
        <select id="filter-tag" class="bg-zinc-900 border border-zinc-700 rounded-lg px-4 py-3 text-base text-zinc-200 focus:outline-none focus:border-amber-600 flex-1 sm:flex-none">
          <option value="">Tous les tags</option>
          <% for (const t of tags) { %>
            <option value="<%= t %>"><%= t %></option>
          <% } %>
        </select>
      <% } %>
    </div>
    <% if (currentPage === 'all') { %>
      <button id="btn-batch-covers" class="px-5 py-3 bg-zinc-800 hover:bg-zinc-700 text-zinc-300 rounded-lg text-base transition-colors whitespace-nowrap">
        Chercher toutes les couvertures
      </button>
      <span id="batch-cover-status" class="text-sm text-zinc-500 self-center hidden"></span>
    <% } %>
  </div>

  <% if (bds.length === 0) { %>
    <p class="text-zinc-500 text-center py-16 text-lg">Aucune BD ici pour l'instant.</p>
  <% } else { %>

    <!-- Pagination info -->
    <div class="flex items-center justify-between mb-4">
      <p id="showing-count" class="text-sm text-zinc-500"></p>
      <div class="flex items-center gap-3" id="pagination-controls"></div>
    </div>

    <!-- Desktop: table -->
    <div class="hidden md:block overflow-x-auto">
      <table class="w-full text-base" id="bd-table">
        <thead>
          <tr class="text-left text-zinc-500 border-b border-zinc-800">
            <th class="pb-3 pr-5 w-16"></th>
            <th class="pb-3 pr-5 cursor-pointer hover:text-zinc-300 select-none text-sm" data-sort="serie">Série <span class="sort-icon"></span></th>
            <th class="pb-3 pr-5 cursor-pointer hover:text-zinc-300 select-none text-sm" data-sort="titre">Titre <span class="sort-icon"></span></th>
            <th class="pb-3 pr-5 w-20 text-center cursor-pointer hover:text-zinc-300 select-none text-sm" data-sort="numero">N° <span class="sort-icon"></span></th>
            <th class="pb-3 pr-5 text-sm">Statut</th>
            <th class="pb-3 pr-5 text-sm">Tags</th>
            <th class="pb-3 pr-5 w-16 text-sm">Lien</th>
            <th class="pb-3 w-44 text-sm"></th>
          </tr>
        </thead>
        <tbody>
          <% for (const bd of bds) { %>
            <tr class="bd-row border-b border-zinc-800/50 hover:bg-zinc-900/50 transition-colors"
                data-serie="<%= bd.serie %>"
                data-status="<%= bd.status %>"
                data-tags="<%= (bd.tags || []).join(',') %>"
                data-search="<%= (bd.serie + ' ' + bd.titre + ' ' + (bd.tags || []).join(' ')).toLowerCase() %>"
                data-sort-serie="<%= bd.serie.toLowerCase() %>"
                data-sort-titre="<%= bd.titre.toLowerCase() %>"
                data-sort-numero="<%= bd.numero != null ? bd.numero : 99999 %>">
              <td class="py-3 pr-4">
                <% if (bd.cover_url) { %>
                  <img src="<%= bd.cover_url %>" alt="" class="w-12 h-16 object-cover rounded shadow-sm" loading="lazy">
                <% } else { %>
                  <div class="w-12 h-16 bg-zinc-800 rounded flex items-center justify-center">
                    <svg class="w-6 h-6 text-zinc-600" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"/></svg>
                  </div>
                <% } %>
              </td>
              <td class="py-3 pr-5 text-zinc-300 font-medium"><%= bd.serie %></td>
              <td class="py-3 pr-5 text-zinc-200"><%= bd.titre %></td>
              <td class="py-3 pr-5 text-center text-zinc-400"><%= bd.numero != null ? bd.numero : '' %></td>
              <td class="py-3 pr-5">
                <select class="status-select bg-transparent border border-zinc-700 rounded-lg px-3 py-1.5 text-sm focus:outline-none focus:border-amber-600
                  <%= bd.status === 'owned' || bd.status === 'owned_no_hs' ? 'text-emerald-400' : '' %>
                  <%= bd.status === 'missing' ? 'text-red-400' : '' %>
                  <%= bd.status === 'in_progress' ? 'text-amber-400' : '' %>
                  <%= bd.status === 'not_owned' ? 'text-zinc-500' : '' %>"
                  data-id="<%= bd.id %>">
                  <% for (const s of statuses) { %>
                    <option value="<%= s.value %>" <%= bd.status === s.value ? 'selected' : '' %>><%= s.label %></option>
                  <% } %>
                </select>
              </td>
              <td class="py-3 pr-5">
                <% if (bd.tags && bd.tags.length > 0) { %>
                  <div class="flex flex-wrap gap-1.5">
                    <% for (const tag of bd.tags) { %>
                      <span class="px-2 py-0.5 bg-zinc-800 rounded-md text-sm text-zinc-400"><%= tag %></span>
                    <% } %>
                  </div>
                <% } %>
              </td>
              <td class="py-3 pr-5 text-center">
                <% if (bd.web_tracking) { %>
                  <a href="<%= bd.web_tracking %>" target="_blank" rel="noopener" class="text-amber-500 hover:text-amber-400 text-lg" title="Suivi web">&#8599;</a>
                <% } %>
              </td>
              <td class="py-3 text-right whitespace-nowrap">
                <a href="/edit/<%= bd.id %>" class="text-zinc-500 hover:text-zinc-300 text-sm mr-2">Modifier</a>
                <form method="POST" action="/duplicate/<%= bd.id %>" class="inline">
                  <button type="submit" class="text-zinc-500 hover:text-zinc-300 text-sm mr-2" title="Dupliquer">Dupli.</button>
                </form>
                <button class="btn-delete text-red-600 hover:text-red-400 text-sm" data-id="<%= bd.id %>" data-titre="<%= bd.titre %>">Suppr.</button>
              </td>
            </tr>
          <% } %>
        </tbody>
      </table>
    </div>

    <!-- Mobile: cards -->
    <div class="md:hidden space-y-4">
      <% for (const bd of bds) { %>
        <div class="bd-row bg-zinc-900/50 border border-zinc-800 rounded-xl p-4 flex gap-4"
             data-serie="<%= bd.serie %>"
             data-status="<%= bd.status %>"
             data-tags="<%= (bd.tags || []).join(',') %>"
             data-search="<%= (bd.serie + ' ' + bd.titre + ' ' + (bd.tags || []).join(' ')).toLowerCase() %>"
             data-sort-serie="<%= bd.serie.toLowerCase() %>"
             data-sort-titre="<%= bd.titre.toLowerCase() %>"
             data-sort-numero="<%= bd.numero != null ? bd.numero : 99999 %>">
          <div class="flex-shrink-0">
            <% if (bd.cover_url) { %>
              <img src="<%= bd.cover_url %>" alt="" class="w-16 h-22 object-cover rounded-lg shadow" loading="lazy">
            <% } else { %>
              <div class="w-16 h-22 bg-zinc-800 rounded-lg flex items-center justify-center">
                <svg class="w-7 h-7 text-zinc-600" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"/></svg>
              </div>
            <% } %>
          </div>
          <div class="flex-1 min-w-0">
            <div class="flex items-start justify-between gap-2">
              <div class="min-w-0">
                <p class="text-base font-medium text-zinc-200 truncate"><%= bd.titre %></p>
                <p class="text-sm text-zinc-400 mt-0.5"><%= bd.serie %><%= bd.numero != null ? ' #' + bd.numero : '' %></p>
              </div>
              <div class="flex items-center gap-2 flex-shrink-0">
                <% if (bd.web_tracking) { %>
                  <a href="<%= bd.web_tracking %>" target="_blank" rel="noopener" class="text-amber-500 text-base">&#8599;</a>
                <% } %>
                <a href="/edit/<%= bd.id %>" class="text-zinc-500 text-sm">Modifier</a>
              </div>
            </div>
            <% if (bd.tags && bd.tags.length > 0) { %>
              <div class="flex flex-wrap gap-1.5 mt-2">
                <% for (const tag of bd.tags) { %>
                  <span class="px-2 py-0.5 bg-zinc-800 rounded-md text-sm text-zinc-500"><%= tag %></span>
                <% } %>
              </div>
            <% } %>
            <div class="mt-3 flex items-center gap-3">
              <select class="status-select bg-transparent border border-zinc-700 rounded-lg px-3 py-1.5 text-sm focus:outline-none focus:border-amber-600
                <%= bd.status === 'owned' || bd.status === 'owned_no_hs' ? 'text-emerald-400' : '' %>
                <%= bd.status === 'missing' ? 'text-red-400' : '' %>
                <%= bd.status === 'in_progress' ? 'text-amber-400' : '' %>
                <%= bd.status === 'not_owned' ? 'text-zinc-500' : '' %>"
                data-id="<%= bd.id %>">
                <% for (const s of statuses) { %>
                  <option value="<%= s.value %>" <%= bd.status === s.value ? 'selected' : '' %>><%= s.label %></option>
                <% } %>
              </select>
              <button class="btn-delete text-red-600 hover:text-red-400 text-sm" data-id="<%= bd.id %>" data-titre="<%= bd.titre %>">Suppr.</button>
            </div>
          </div>
        </div>
      <% } %>
    </div>

  <% } %>
</main>

<!-- Delete confirmation modal -->
<div id="delete-modal" class="hidden fixed inset-0 z-[60] flex items-center justify-center bg-black/60 backdrop-blur-sm">
  <div class="bg-zinc-900 border border-zinc-700 rounded-xl p-8 mx-4 max-w-md w-full shadow-xl">
    <h3 class="text-lg text-zinc-100 font-medium mb-3">Supprimer cette BD ?</h3>
    <p class="text-base text-zinc-400 mb-6">&laquo; <span id="delete-modal-titre"></span> &raquo; sera définitivement supprimée.</p>
    <div class="flex justify-end gap-4">
      <button id="delete-cancel" class="px-5 py-2.5 text-base text-zinc-400 hover:text-zinc-200 rounded-lg hover:bg-zinc-800 transition-colors">Annuler</button>
      <button id="delete-confirm" class="px-5 py-2.5 text-base bg-red-600 hover:bg-red-500 text-white rounded-lg transition-colors">Supprimer</button>
    </div>
  </div>
</div>

<%- include('partials/foot') %>
